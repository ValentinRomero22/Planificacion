import { Header } from "../components/Header"
import { Grid } from "../components/Grid"

export const Home = () => {
    return (
        <main>
            <Header />
            <Grid />
        </main>
    )
}